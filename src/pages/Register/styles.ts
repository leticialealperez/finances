import { Theme } from '@mui/material';

const useStyles = (theme: Theme) => ({
  titleInfo: {
    color: '#777',
  },
  actions: {
    justifyContent: 'space-between',
  },
});

export { useStyles };
